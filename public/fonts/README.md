# Visby CF font

The licensed Visby CF font files are intentionally not included. The CSS already uses `Visby CF` as the first font in the stack.

Before launch, add your licensed webfont files here and define them with `@font-face` at the top of `app/globals.css` using `font-display: swap`.
